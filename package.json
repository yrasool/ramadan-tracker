{
  "name": "ramadan-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "node scripts/check-build-config.mjs",
    "ci": "npm run check && npm run build",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke-dist.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.1.1",
    "vite": "^5.0.0"
  }
}
